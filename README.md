# Asteroids

Minimal Asteroids clone built on `mini-arcade-core` with the pygame backend.

## Run

```bash
python manage.py
```

## Controls

- `Left` / `Right`: rotate ship
- `Up`: thrust
- `Space`: fire
- `Esc`: pause
